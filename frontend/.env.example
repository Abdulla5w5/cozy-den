# The frontend talks to the backend via the Vite dev proxy (/api -> :4000),
# so no API URL is needed in development. For a production build served
# separately, configure your reverse proxy to route /api to the backend.
